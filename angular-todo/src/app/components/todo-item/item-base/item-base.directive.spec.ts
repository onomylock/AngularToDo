import { ItemBaseDirective } from './item-base.directive';

describe('ItemBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemBaseDirective();
    expect(directive).toBeTruthy();
  });
});
